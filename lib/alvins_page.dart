import 'package:flutter/material.dart';

class AlvinsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Alvin',
        style: TextStyle(fontWeight: FontWeight.w900, fontSize: 30),
      ),
    );
  }
}
